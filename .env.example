# Copy to .env.local and fill in to enable hosted (ElevenLabs) voices for Benny
# in the Book Talk Badges prototype. Get a key at https://elevenlabs.io
# (Profile → API Keys). Without it, the chat uses the browser's built-in voice.
VITE_ELEVENLABS_API_KEY=
