import React from 'react'
import { BackBar } from 'bs-prototypes'

export const ToInsights = () => <BackBar label="Back to Insights" onClick={() => {}} />

export const ToRoster = () => <BackBar label="Back to Class Roster" onClick={() => {}} />

export const ToChallenge = () => <BackBar label="Back to Summer Reading Challenge" onClick={() => {}} />
